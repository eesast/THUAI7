using Protobuf;
using TMPro;
public class Debugger : SingletonMono<Debugger>
{
    void Start()
    {
        // InputControl.GetInstance().AfterInputPlaySpeed("3");
        // InputControl.GetInstance().AfterInputFilename("D:\\Users\\hanzhifeng\\Downloads\\playback.thuaipb");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
